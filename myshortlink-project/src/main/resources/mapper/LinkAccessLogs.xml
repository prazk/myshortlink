<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prazk.myshortlink.project.mapper.LinkAccessLogsMapper">

    <insert id="recordAccessLogs">
        INSERT INTO
            t_link_access_logs (short_uri, user, browser, os, device, ip, create_time, del_flag)
        VALUES (#{et.shortUri}, #{et.user}, #{et.browser}, #{et.os}, #{et.device}, #{et.ip}, #{et.createTime}, #{et.delFlag})
    </insert>

    <select id="selectTopIP" resultType="com.prazk.myshortlink.project.pojo.vo.LinkTopIPStatsVO">
        SELECT ip, COUNT(*) AS cnt
        FROM t_link_access_logs
        WHERE short_uri = #{shortUri}
        AND create_time BETWEEN #{startDateTime} AND #{endDateTime}
        GROUP BY ip
        ORDER BY cnt DESC
        LIMIT 10
    </select>
</mapper>